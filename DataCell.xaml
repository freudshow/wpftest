<Window x:Class="WpfApp2.DataCell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="DataCell" Height="450" Width="800"
        Loaded="Window_Loaded">
    <Grid>
        <DataGrid x:Name="excelDataGrid"
                  Margin="10"
                  IsReadOnly="False"
                  CanUserAddRows="True"
                  CanUserDeleteRows="True"
                  SelectionUnit="CellOrRowHeader"
                  ClipboardCopyMode="IncludeHeader"
                  AllowDrop="True"
                  MouseDown="ExcelDataGrid_MouseDown"
                  MouseMove="ExcelDataGrid_MouseMove"
                  MouseUp="ExcelDataGrid_MouseUp"
                  KeyDown="ExcelDataGrid_KeyDown">

            <!-- 右键菜单：插入行/列 -->
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="插入行（上方）" Click="InsertRowAbove_Click" />
                    <MenuItem Header="插入行（下方）" Click="InsertRowBelow_Click" />
                    <Separator />
                    <MenuItem Header="插入列（左侧）" Click="InsertColumnLeft_Click" />
                    <MenuItem Header="插入列（右侧）" Click="InsertColumnRight_Click" />
                </ContextMenu>
            </DataGrid.ContextMenu>

            <!-- 单元格样式：添加填充柄（Excel右下角小方块） -->
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Grid>
                                    <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                                    <!-- 填充柄：仅在选中单元格时显示 -->
                                    <Rectangle x:Name="fillHandle"
                                               Width="8" Height="8"
                                               HorizontalAlignment="Right"
                                               VerticalAlignment="Bottom"
                                               Fill="Gray"
                                               Visibility="Collapsed"
                                               Cursor="SizeNWSE" />
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="fillHandle" Property="Visibility" Value="Visible" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.CellStyle>
        </DataGrid>
    </Grid>
</Window>